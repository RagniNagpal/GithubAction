name: Needs Usage

on: push
    
jobs:

  run-test:
    runs-on: ubuntu-latest

    steps:
      - name: Print message 1
        run: echo "Running tests..."

      - name: Print message 2
        run: echo "Tests completed"

  deploy-app:
    runs-on: ubuntu-latest

    needs: run-test   #  पहले ये job complete होगी

    steps:
      - name: Deploy step
        run: echo "Deploying application..."